import 'package:flutter/material.dart';

class SelectProfilePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Select Profile"),
      ),
      body: Center(
        child: Text("This is the Select Profile Page"),
      ),
    );
  }
}
